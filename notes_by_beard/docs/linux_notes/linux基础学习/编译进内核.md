###### Kconfig,Makefile,.config的作用和区别
1. Kconfig为make menuconfig提供了图形化页面，使得能够进行图形化配置。
2. .config提供了那些能够被修改的变量，确定了那些文件被编译哪些文件不被编译。
3. Makefile根据变量的值做出相应的编译行为。
###### 内核文件中的多个Kconfig文件是怎么联合到一起的
通过修改上层目录下的文件，再修改需编译文件夹的的编译配置文件，具体过程有些复杂。



