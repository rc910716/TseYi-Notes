###### __label__
__label__ 是 GNU C 扩展中用于在语句块内部定义局部标签的关键字，主要用于在复杂代码块（如宏定义、嵌套语句）中实现局部跳转，避免标签的作用域污染全局。
	使用 __label__ 定义的标签仅在当前语句块（{ ... }）内可见，不会与块外的同名标签冲突。
	宏定义中经常需要跳转逻辑，__label__ 可避免标签重名问题（多个宏实例展开时不会冲突）。若直接使用普通标签（不加 __label__），多次展开宏时会出现 “标签重定义” 错误，而 __label__ 可解决此问题。